<Window x:Class="NgoDongQuan_a2.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Email:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" MinWidth="200"/>

        <TextBlock Text="Password:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" MinWidth="200"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <Button Content="Login" Command="{Binding LoginCommand}" Grid.Row="2" Grid.ColumnSpan="2" Height="30" Margin="0,10,0,0"/>

        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Grid.Row="3" Grid.ColumnSpan="2" Margin="0,10,0,0"/>
    </Grid>
</Window> 